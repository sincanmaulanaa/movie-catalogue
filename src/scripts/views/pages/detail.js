/* eslint-disable no-tabs */
const Detail = {
  async render() {
    return `
		<h2>Detail Page</h2>
	`;
  },

  async afterRender() {
    // fungsi ini akan dipanggil setelah render()
  },
};

export default Detail;
